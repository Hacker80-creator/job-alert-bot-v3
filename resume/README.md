# Resume template

`master_resume.docx` is the immutable formatting and factual source of truth.
The bot always edits a copy; generated resumes never overwrite this master.
`master_resume.sha256` makes accidental master changes fail closed.
